// Search tRPC router (F-143)
// Procedures: query (full-text search across all content types via PostgreSQL FTS).
// Public: query.
// Supports Sinhala, Tamil, and English via pg_trgm + unaccent + language-specific dictionaries.
