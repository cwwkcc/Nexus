// Audit tRPC router (F-055)
// Procedures: list (with filters by user/entity/action/date), getEntry.
// Admin only: all procedures (F-082).
// Read-only — no create/update/delete procedures. Writes happen only via tRPC middleware.
