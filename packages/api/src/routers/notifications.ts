// Notifications tRPC router (F-055)
// Covers the AnnouncementBanner system (F-159).
// Procedures: getActive, create, deactivate, expire, listAll.
// Public: getActive.
// Protected (Editor+): create, deactivate.
// Admin only: expire, listAll.
