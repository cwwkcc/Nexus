// Page Content tRPC router (F-058)
// Procedures: getByScope, update (upsert + version snapshot).
// Public: getByScope (with English fallback for missing locales — F-074).
// Protected (Editor+): update.
