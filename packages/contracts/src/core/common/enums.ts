// packages/contracts/src/core/common/enums.ts
//
// Shared enum schemas reused in 2+ feature areas.
// Feature-specific enums belong in their own feature file, not here.
//
// Should contain:
//   PublishStatusSchema  — z.enum(['draft', 'published', 'archived'])
//   VisibilitySchema     — z.enum(['public', 'private', 'unlisted'])
//   PrioritySchema       — z.enum(['low', 'medium', 'high'])
//   SortOrderSchema      — z.enum(['asc', 'desc'])
//   PublishStatus        — z.infer type
//   Visibility           — z.infer type
//   Priority             — z.infer type
//   SortOrder            — z.infer type



// TODO: implement

export type Enums = unknown;
