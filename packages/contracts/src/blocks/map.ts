// packages/contracts/src/blocks/map.ts
//
// Map block — Google Maps iframe embed.
//
// Should contain:
//   MapSchema — embedUrl (Google Maps embed src URL), title?,
//               height? (number px, default 400), caption?
//   MapData   — z.infer type
//
// Notes:
//   The school's primary map URL is also in SiteSettings as 'school.mapEmbedUrl'.
//   This block is for one-off map embeds on arbitrary pages (e.g. facilities).



// TODO: implement

export type Map = unknown;
