// New Society form (F-153). Logo + banner upload.

import { AdminPlaceholder } from '@/features/shell/AdminPlaceholder';

export default function Page() {
  return <AdminPlaceholder title="societies / new" />;
}
