// Admin Dashboard (F-149)
// Content counts, recent activity (last 10 audit entries), quick actions, pending items.
// Pending count links to Alumni module filtered by status: pending (F-167).

import { AdminPlaceholder } from '@/features/shell/AdminPlaceholder';

export default function Page() {
  return <AdminPlaceholder title="dashboard" />;
}
