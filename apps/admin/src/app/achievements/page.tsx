// Achievement Module list (F-168). Filterable by year, category.

import { AdminPlaceholder } from '@/features/shell/AdminPlaceholder';

export default function Page() {
  return <AdminPlaceholder title="achievements" />;
}
