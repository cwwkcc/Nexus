// News Module list view (F-150)
// List with status badges, search, category filter, date filter.
// Bulk actions: publish, archive, delete.
// "New article" button links to /news/new.

import { AdminPlaceholder } from '@/features/shell/AdminPlaceholder';

export default function Page() {
  return <AdminPlaceholder title="news" />;
}
