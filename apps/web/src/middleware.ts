// Next.js middleware for apps/web (F-069)
// Handles next-intl locale routing: redirects bare / to /en (default locale).
// Does NOT apply authentication — the public site has no auth layer (F-062).
