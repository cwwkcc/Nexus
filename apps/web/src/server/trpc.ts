// Server-side tRPC caller for apps/web (RSC usage)
// Creates a caller using the server-side context so RSC pages can call tRPC procedures
// without an HTTP round trip. Used in generateStaticParams and page Server Components.
